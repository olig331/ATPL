import React from 'react'

export const AllFixtures: React.FC = () => {

  const key: string = "";
  
  const allFixturesAPI = async () =>{
    const response = fetch("");
    const data = await response;
  }

  return (
    <div>
      
    </div>
  );
};
